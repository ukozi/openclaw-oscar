# openclaw-oscar

An OpenClaw channel plugin that signs an agent into an Open OSCAR Server as a native client.

This is an early scaffold. It registers the channel and does nothing else yet. Setup and usage notes arrive with the first release.

MIT licensed.
